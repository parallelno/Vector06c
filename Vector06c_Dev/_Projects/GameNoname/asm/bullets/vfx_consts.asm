	; 
; vfx consts

; animation speed (the less the slower, 0-255, 255 means the next frame is almost every update)
VFX_ANIM_SPEED		= 200

; statuses
VFX_STATUS_DEFAULT	= 0 ; we need to specify at least one status durig init func